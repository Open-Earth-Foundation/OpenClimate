apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclimate-harmonize-persistentvolumeclaim
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
